# ------------------------------
# Node / Expo
# ------------------------------
node_modules/
.expo/
dist/
npm-debug.*
web-build/
expo-env.d.ts

# Metro cache
metro-cache/
.metro-cache-*/
.metro-health-check*

# ------------------------------
# Assets (REQUIRED for EAS builds)
# ------------------------------
# Do NOT ignore assets — icons, splash screens, etc. must be committed
# (intentionally no "assets/" ignore rule)

# ------------------------------
# iOS / Android (KEEP TRACKED)
# ------------------------------
# Native folders MUST be committed for EAS
# (do not ignore ios/ or android/)

# ------------------------------
# macOS
# ------------------------------
.DS_Store

# ------------------------------
# Secrets / Signing Keys (DO NOT COMMIT)
# ------------------------------
credentials.json
google.json

apple.p8
my-release-key.jks

*.jks
*.p8
*.p12
*.pem
*.key
*.mobileprovision

.env
.env.*